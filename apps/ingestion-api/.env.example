# Copy to `.env` when running ingestion-api directly from this directory.
# docker-compose sets equivalent values in the service environment.

# Required: no default.
MONGODB_URL=mongodb://localhost:27017
STORAGE_ROOT=./storage
CREDENTIALS_DIR=../../credentials

# Required while RESOLUTION_PUSH_ENABLED keeps its default true value.
RESOLUTION_API_URL=http://localhost:8000
